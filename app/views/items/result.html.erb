

<h1 class="title">検索結果一覧</h1>
      <table class="table_result">
        <thead>
         <% @items.each do |item| %>
           <tr>
            <td class="image_result_display"><%= link_to item_path(item) do %>
              <%#= attachment_image_tag item, :image, :fill,150,150, format: 'jpeg', size: "120x120", fallback: "sake2.jpg" %>
              <% end %></td>
            <td class="album_result_display"><%= item.name %></br>
              <%= item.introduction %></td>
          </tr>
        </thead>
        <% end %>
      </table>